export default interface userData {
  name: string;
  status: boolean;
  creationDate: string;
  dueDate: string;
}

export interface updatedCards {
  id: string;
  name: string;
  status: boolean;
  creationDate: string;
  dueDate: string;
}
